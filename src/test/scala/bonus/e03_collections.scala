package bonus

import support.HandsOnSuite

class e03_collections extends HandsOnSuite {
  /**
    * More collection functions :
    *   - headOption, lastOption, take, drop, distinct, flatten, forall
    *   - min, max, sum, reverse, sortBy
    *   - partition, sliding
    *   - zip, zipWithIndex, unzip, grouped
    */

  /**
    * Covariance
    */
}
